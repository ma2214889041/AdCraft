# AdCraft 认证系统使用指南

## 🎉 功能概览

AdCraft 现在已经完全集成了 Firebase Authentication，支持以下功能：

- ✅ 邮箱密码注册和登录
- ✅ Google OAuth 一键登录
- ✅ 用户状态管理
- ✅ 安全的登出功能
- ✅ 用户信息显示
- ✅ 表单验证和错误提示

## 🚀 快速开始

### 1. 启用 Firebase 认证方式

在使用前，需要在 Firebase Console 中启用认证方式：

**邮箱/密码认证：**
1. 访问 [Firebase Console](https://console.firebase.google.com/)
2. 选择项目 `ai-adcraft`
3. 左侧菜单 → Authentication → Sign-in method
4. 点击 "Email/Password"
5. 启用并保存

**Google 登录（可选）：**
1. 在同一页面点击 "Google"
2. 启用并保存
3. 设置项目的公开名称

### 2. 启动应用

```bash
npm run dev
```

应用将在 http://localhost:3000 启动

### 3. 测试认证流程

#### 场景 1：从首页开始
1. 访问首页（Landing Page）
2. 点击 "CREATE" 按钮或输入广告描述
3. 系统会自动跳转到登录/注册页面

#### 场景 2：直接访问认证页面
1. 在代码中修改 `App.tsx` 的初始视图
2. 将 `currentView` 设置为 `'auth'`

## 📝 使用示例

### 注册新账户

1. 在认证页面点击 "Sign Up" 标签
2. 填写以下信息：
   - **Full Name**: 你的名字
   - **Email**: 有效的邮箱地址
   - **Password**: 至少 6 位字符
   - **Confirm Password**: 确认密码
3. 点击 "Create Account"
4. 注册成功后自动登录并跳转到 Dashboard

### 使用邮箱登录

1. 在认证页面保持 "Sign In" 标签
2. 输入：
   - **Email**: 注册时使用的邮箱
   - **Password**: 密码
3. 点击 "Sign In"
4. 登录成功后跳转到 Dashboard

### 使用 Google 登录

1. 点击 "Continue with Google" 按钮
2. 选择 Google 账户
3. 授权应用访问
4. 自动登录并跳转到 Dashboard

### 登出

1. 点击页面右上角的用户头像/名字
2. 在下拉菜单中点击 "Sign Out"
3. 自动返回首页

## 🔒 安全特性

### API 密钥保护
- API 密钥存储在 `.env` 文件中
- `.env` 已添加到 `.gitignore`，不会提交到版本控制
- 生产环境使用环境变量管理敏感信息

### 表单验证
- ✅ 邮箱格式验证
- ✅ 密码长度验证（至少 6 位）
- ✅ 密码确认匹配验证
- ✅ 必填字段检查

### 错误处理
系统会显示友好的错误提示：
- 无效的邮箱地址
- 密码太弱
- 邮箱已被注册
- 用户不存在
- 密码错误
- 网络错误等

## 🎨 用户界面特性

### 认证页面
- 现代化设计，深色主题
- 平滑的表单切换动画
- 实时表单验证反馈
- 加载状态指示器
- Google 品牌按钮

### 用户菜单
- 显示用户头像（Google 登录）或姓名缩写
- 显示完整用户名和邮箱
- 下拉菜单支持点击外部关闭

## 🧪 测试建议

### 测试场景

1. **注册流程**
   - [ ] 使用有效信息注册新账户
   - [ ] 尝试注册已存在的邮箱（应显示错误）
   - [ ] 使用弱密码注册（应显示错误）
   - [ ] 密码不匹配（应显示错误）

2. **登录流程**
   - [ ] 使用正确的账户信息登录
   - [ ] 使用错误的密码登录（应显示错误）
   - [ ] 使用不存在的邮箱登录（应显示错误）

3. **Google 登录**
   - [ ] 首次使用 Google 登录（自动注册）
   - [ ] 已有账户的 Google 登录

4. **用户状态**
   - [ ] 登录后刷新页面（应保持登录状态）
   - [ ] 登出后访问受保护页面（应跳转登录）
   - [ ] Header 正确显示用户信息

5. **导航流程**
   - [ ] 未登录时点击 CREATE（跳转到登录页）
   - [ ] 登录后点击 CREATE（跳转到创建页）
   - [ ] 登出后自动返回首页

## 🐛 常见问题

### 问题 1：显示 "Invalid API key" 错误
**解决方案：**
- 确保 `.env` 文件存在且包含所有必需的环境变量
- 重启开发服务器
- 检查 Firebase Console 中的 API 密钥是否正确

### 问题 2：Google 登录失败
**解决方案：**
- 在 Firebase Console 中启用 Google 认证
- 确保在 Authorized domains 中添加了 `localhost`
- 检查浏览器是否阻止了弹出窗口

### 问题 3：注册后无法登录
**解决方案：**
- 检查 Firebase Console → Authentication → Users
- 确认用户已成功创建
- 尝试使用密码重置功能

### 问题 4：用户头像不显示
**解决方案：**
- Google 登录会自动使用 Google 头像
- 邮箱注册会显示用户名缩写
- 检查控制台是否有图片加载错误

## 📚 技术文档

### 认证服务 API

```typescript
import {
  loginWithEmail,
  registerWithEmail,
  loginWithGoogle,
  logout,
  getCurrentUser,
  onAuthChange
} from './services/authService';

// 注册
await registerWithEmail('user@example.com', 'password123', 'John Doe');

// 登录
await loginWithEmail('user@example.com', 'password123');

// Google 登录
await loginWithGoogle();

// 登出
await logout();

// 获取当前用户
const user = getCurrentUser();

// 监听认证状态
const unsubscribe = onAuthChange((user) => {
  if (user) {
    console.log('User logged in:', user.email);
  } else {
    console.log('User logged out');
  }
});
```

### 环境变量

```.env
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id
```

## 🎯 下一步开发

可以考虑添加的功能：

- [ ] 密码重置功能
- [ ] 邮箱验证
- [ ] 用户个人资料编辑
- [ ] 更多 OAuth 提供商（Facebook, Twitter 等）
- [ ] 记住我功能
- [ ] 两步验证
- [ ] 用户活动日志

## 📞 需要帮助？

如果遇到问题：
1. 查看浏览器控制台的错误信息
2. 检查 Firebase Console 中的日志
3. 参考 [Firebase Authentication 文档](https://firebase.google.com/docs/auth)
4. 查看项目的 `services/authService.ts` 中的错误处理

---

**祝你使用愉快！** 🚀
